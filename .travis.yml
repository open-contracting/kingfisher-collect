sudo: false
language: python
cache: pip
python:
  - "3.6"
install:
  - pip install -r requirements_dev.txt
script:
  - flake8
  - python -m pytest --cov kingfisher_scrapy
after_success:
  - coveralls
