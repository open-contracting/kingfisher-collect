sudo: false
language: python
cache: pip
python:
  - "3.6"
install:
  - pip install -r requirements_dev.txt
script:
  - flake8
  - isort
  - python -m pytest --cov kingfisher_scrapy tests
after_success:
  - coveralls
